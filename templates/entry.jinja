@{{ bibliography[key].type.upper() }} { {{-key-}},
    {% for field in bibliography[key].fields %}
    {% if field == "title" and bibliography[key].fields["title"][0] == '{' %}
    title = {{ bibliography[key].fields[field] }},
    {% else %}
    {{ field.lower() }} = { {{-bibliography[key].fields[field]-}} },
    {% endif %}
    {% endfor %}
}
